services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      # 開發模式：禁用 CSP、便於本地聯調
      NODE_ENV: development
      DISABLE_CSP: "true"
      RUN_MIGRATIONS: "false"
